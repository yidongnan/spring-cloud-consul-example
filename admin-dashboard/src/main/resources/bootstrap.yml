server:
  port: 8040

spring:
  application:
    name: admin-dashboard
  cloud:
    consul:
      discovery:
        prefer-ip-address: true
      host: 127.0.0.1
      port: 8500
  boot.admin.discovery:
    ignored-services: consul